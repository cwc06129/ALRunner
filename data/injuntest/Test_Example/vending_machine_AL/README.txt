해당 폴더는 실험을 진행하고자 하는 코드에 대한 model.c와 model.h를 저장하는 폴더입니다.
trace generation을 위해 make_trace.c 또한 해당 파일에 넣어놓았습니다.

<trace generation을 위한 과정>
./generate_trace.sh 를 실행시킨다.

<ActiveLearning 과정에서 생성되는 파일들>
code_proof.c
code.c